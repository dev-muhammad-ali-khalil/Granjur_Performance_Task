import React from "react";
import Box from "@mui/material/Box";

const GridRight = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        gap: "16px",
      }}
    >
      Grid Right
    </Box>
  );
};

export default GridRight;
